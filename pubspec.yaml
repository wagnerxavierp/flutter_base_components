name: flutter_base_components
description: "A new Flutter package project."
version: 0.0.2
homepage:

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
    
  #Android iOS linux macOS web Windows:
  get: ^4.7.2
  either_dart: ^1.0.0
  reorderables: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
